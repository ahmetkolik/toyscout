# 29 Temmuz 2026 — GSC / YouTube / TikTok denetim kaydı

Bu dosya siteye deploy edilmez (`notes/` → `.vercelignore`). Amaç: o gün ne yapıldığının
ve neyin açık kaldığının kalıcı kaydı.

---

## A. Yapılan işler

### A1. Yapılandırılmış veri: fiyatsız `offers` hatası — KAPANDI
- **Tetikleyen:** 29 Tem 07:17'de gelen iki GSC maili (Merchant listings + Product snippets),
  kritik hata `Missing field "price" (in "offers")`.
- **Kök neden:** `index.html` içindeki `updateProductLd()`, ürünün fiyatı olmasa bile
  `offers` nesnesini basıyordu. Fiyatsız Offer = geçersiz yapılandırılmış veri.
- **Düzeltme:** `offers` artık yalnızca fiyat biliniyorsa yazılıyor.
- **Commit:** `ddd9818` · **GSC:** "VALIDATE FIX" başlatıldı (durum *Started*).

### A2. Yapılandırılmış veri: 150+ karakter `name` uyarısı — KAPANDI
- **Uyarı:** `Invalid string length in field "name"` (30 öğe). Google Product `name`
  alanını 150 karakterle sınırlıyor; kataloğun 97 adından 56'sı daha uzundu.
- **Düzeltme:** yeni `ldName()` yardımcı fonksiyonu adları kelime sınırında 150'ye kırpıyor
  (hem `Product` hem `ItemList` için).
- **Commit:** `557e499`.

### A3. Ana sayfadaki statik JSON-LD bayattı — KAPANDI
`<head>` içindeki 5 statik ürün bloğu `js/data.js` ile eşitlendi:

| ASIN | Yorum sayısı | Fiyat |
|---|---|---|
| B0BDQXLT4C | 6605 → 9243 | $8.99 → $9.99 |
| B0D9ZTW7PS | 15525 → 4436 (puan 4.0 → 4.1) | `offers` bloğu kaldırıldı (buybox yok) |
| B07PQFT83F | 7141 → 49086 | — |
| B00E4AMFKK | 2576 → 794 | $5.82 → $4.72 |
| B098PMWSVS | 1222 → 6630 | — |

**Commit:** `557e499` (A2 ile aynı).

### A4. Katalog: 13 fiyatsız ürün canlı Amazon ile karşılaştırıldı
Yöntem: amazon.com sekmesinde `javascript_tool` ile same-origin `fetch('/dp/<ASIN>')`
(captcha yok), sonra şüpheli olanlar sayfayı açarak doğrulandı.

- 10 ürün gerçekten **"Currently unavailable"** → fiyatsız kalması doğru.
- **B0CCW6WLJ9** (SwimWays Spring Float) → `$39.99`, stokta. Eklendi.
- **B0F2HVLZ12** (NatNarr Bow Cake Toppers) → `$6.89`, stokta; `rc` 968 → 981.
- **B0H1GX1KJB** (Sugar Dumpling Squishy) → Amazon'da **HTTP 404, ölü listing**. Silindi.

Sonuç: novelty 17 → 16, toplam 98 → **97 ürün**; `sitemap.xml`'den `/product/novelty/16`
çıkarıldı (124 → **123 URL**). **Commit:** `ddd9818`.

> **Tuzak:** ürün silmek index tabanlı URL'leri kaydırıyor. Silinen öğeden sonraki tüm
> ürünlerin `/product/<kategori>/<n>` adresleri değişiyor — sitemap'i de güncellemek şart.

### A5. Sitemap okunmuyor → yeni `sitemap-products.xml`
- **Sorun:** `/sitemap.xml` GSC'de Submitted 29 Tem ama **Last read 26 Tem, Discovered 25**.
  Canlı dosya doğru (curl ile doğrulandı) — sorun bizde değil, Google okumuyor.
- **Yapılan:** yalnızca 97 ürün URL'si içeren yepyeni bir `sitemap-products.xml` üretildi
  (lastmod `2026-07-29`), `robots.txt`'ye ikinci `Sitemap:` satırı eklendi, GSC'ye gönderildi.
- **Commit:** `c64bcc7`.
- **Not:** ilk gönderim **"Couldn't fetch"** döndü — Vercel edge cache'i o an hâlâ 404
  veriyordu (dosya ~1 dk sonra 200'e döndü). Yeniden gönderildi.
  **Ders: deploy sonrası GSC'ye göndermeden önce URL'in 200 döndüğünü curl ile doğrula.**

### A6. Denetlenip temiz çıkanlar
- Product snippets **44 geçerli / 0 hatalı** (28 Tem'de 24'tü → Google ürün sayfalarını
  taramaya başlamış).
- Review snippets **128 geçerli / 0 hatalı**.
- Merchant listings 43 geçerli / 1 hatalı (o 1 = A1, düzeltildi).
- HTTPS 40, Core Web Vitals hâlâ "No data" (trafik az).

### A7. Sosyal medya denetimi
- **YouTube (@toyscoutnet):** 17 Short + 1 uzun-format. 10 Short yayında (en yenisi 24 Tem,
  06-B). **7 yeni Short yüklenmiş, hepsi "Planlandı"**, hiçbiri canlı değil.
- **TikTok (@toyscoutnet):** 13 yayında + 6 zamanlanmış. Aynı 7 videodan biri **bugün
  11:40'ta yayınlanmış** ("Can you guess the color by smell?") ve ~1 saatte **95 izlenme**.
- 27 Tem'de "hiç üretilmedi" denen 9 slottan **7'si tamamlandı**.

Yeni videoların takvimi:

| Tarih | Video | Slot |
|---|---|---|
| 29 Tem | The magnetic board that keeps toddlers quiet | 07-A Kikidex |
| 29/30 Tem | Can you guess the color by smell? | 07-B Mr. Sketch |
| 30 Tem | The $7 baby toy with 47,000 reviews | 08-A Oball |
| 31 Tem | The suction toy that ends high-chair meltdowns | 08-B ALASOU |
| 31 Tem | Markers with NO caps to lose | 09-B Crayola Clicks |
| 31 Tem | 500 stickers = 2 hours of quiet | 10-A sticker kitabı |
| 1 Ağu | The gadget that puts babies to sleep in minutes | 10-B beyaz gürültü |

---

## B. Açık kalan görevler

### B1. ⏰ BUGÜN 21:00'DEN ÖNCE — TikTok açıklama düzeltmesi
29 Tem 21:00'e zamanlı videonun açıklaması şöyle başlıyor:

```
07-A Kikidex Manyetik Çizim Tahtası The magnetic board that keeps toddlers quiet ✏️ ...
```

İç kampanya slot kodu (`07-A`) metne sızmış. Olması gereken:

```
The magnetic board that keeps toddlers quiet ✏️ moms love it — link in bio 🔗 #tiktokmademebuyit #amazonfinds #toddlertoys #montessori #momsoftiktok
```

**Neden ben yapamadım:** TikTok Studio web'de düzenleme butonu kilitli —
*"Ticari içeriğe sahip videolar yalnızca TikTok uygulamasında düzenlenebilir."*
→ **Telefondaki TikTok uygulamasından yapılmalı.**

Not: Türkçe ürün adı öneki eski gönderilerde de var (Buzz Lightyear, LCD Yazı Tableti,
John Deere...), yani alışkanlık. Asıl anormallik `07-A` kodu — aynı sızıntı 16 Tem'deki
ilk gönderide `01-A` olarak da olmuş.

### B2. ⏰ BU AKŞAM 22:00'DEN SONRA — indeksleme isteği turu
Bugün ~12:00'de denendi → **"Quota Exceeded"**. Kota 24 saat kayan pencerede çalışıyor,
takvim günü değil; 28 Tem akşamı (~22:00) harcanan kota gün içinde açılmadı.
Öncelik sırası: ürün sayfaları → kategori → blog. Günlük ~10 URL.

### B3. Yarın — `sitemap-products.xml` durumu
GSC → Sitemaps ekranında `/sitemap-products.xml` satırına bak:
- **"Success" + Discovered pages 97** olduysa darboğaz açıldı.
- Hâlâ **"Couldn't fetch"** ise: satırı sil, birkaç saat bekle, yeniden gönder.
- `/sitemap.xml` için de **Last read tarihinin 29 Tem'e** çıkıp çıkmadığına bak.

### B4. Yarın/sonrası — VALIDATE FIX sonucu
Merchant listings → `Missing field "price"` doğrulaması *Started* durumda. Google'ın
"Passed" demesi birkaç gün sürer. `Invalid string length in field "name"` uyarısı için
doğrulama başlatılamadı (GSC uyarı seviyesindekilerde hep izin vermiyor) — bir sonraki
taramada kendiliğinden düşmeli.

### B5. Üretilmemiş 2 video slotu
- **06-A** — Woody Aksiyon Figürü
- **09-A** — SEREED denge bisikleti

### B6. Eski borç: 04-A format hatası
Candy Land videosu (`dnkiQr9BkHI`) hâlâ **3:31 uzun-format**, Shorts değil. 5 denetimdir
düzeltilmedi. Düzeltme = 15-20 sn'lik Shorts sürümünü yeniden yükleyip eskisini kaldırmak.

### B7. Bilinçli olarak yapılmayanlar (karar kaydı)
GSC'de açık kalan kritik olmayan uyarılar:
- `hasMerchantReturnPolicy` yok (44 öğe)
- `shippingDetails` yok (44 öğe)

**Yapılmadı çünkü:** ToyScout satıcı değil, affiliate. Amazon'un iade/kargo şartlarını
kendi yapılandırılmış verimizde beyan etmek yanlış bilgi olur. Bırakılması bilinçli.

`No global identifier (gtin, brand)` (2 öğe) — küçük, ertelendi.

---

## C. Deploy edilen commit'ler (hepsi canlı, curl ile doğrulandı)

| Commit | İçerik |
|---|---|
| `ddd9818` | fiyatsız `offers` düzeltmesi + 2 fiyat + ölü ASIN silindi + sitemap 123 URL |
| `557e499` | `ldName()` 150 karakter kırpma + ana sayfa statik JSON-LD tazelendi |
| `c64bcc7` | `sitemap-products.xml` (97 URL) + `robots.txt` |

Deploy yöntemi: GitHub Git Data API ile tek commit (`gh api` → blobs → tree → commit →
ref PATCH). Repo klasörü git deposu değil, `gh repo clone` frames/ yüzünden timeout oluyor.

**Not:** Vercel edge cache `must-revalidate` olduğu için deploy sonrası ilk birkaç istek
eski içeriği dönebilir; doğrularken `?cb=$RANDOM` ekle.

## D. Yedekler
Değişiklik öncesi kopyalar oturum scratchpad'inde:
`data.js.bak-0729`, `sitemap.xml.bak-0729`
